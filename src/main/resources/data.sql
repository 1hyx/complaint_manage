CREATE table department(
    id char,
    name char,
    comNum int,
    finishComNum int,
    processComNum int,
    freshComNum int
    );

CREATE TABLE order(
    id char,
    comDate date,
    comType char,
    comDesc char,
    comTitle char
);
